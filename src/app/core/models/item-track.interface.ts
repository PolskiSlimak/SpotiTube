import { TrackData } from "./track-data.interface";

export class ItemTrack {
  added_at: string;
  added_by: any;
  is_local: boolean;
  primary_color: any;
  track: TrackData;
  video_thumbnail: any;
}
